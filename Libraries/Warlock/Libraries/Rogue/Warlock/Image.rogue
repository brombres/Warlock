module Warlock

class Image
  GLOBAL METHODS
    method create( asset:Asset )->Image
      return TextureImage( Texture(asset) )

  PROPERTIES
    size : XY

  METHODS
    method is_ready->Logical
      return true

    method size->XY
      return @size

    method texture->Texture
      return null
endClass
