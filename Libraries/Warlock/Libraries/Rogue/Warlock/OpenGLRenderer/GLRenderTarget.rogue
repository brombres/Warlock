module Warlock

class GLRenderTarget : RenderTarget
  PROPERTIES

  METHODS
    method init
      is_main_display = true

    method init( size:XY )
      # Offscreen buffer creation
      noAction # TODO
endClass
