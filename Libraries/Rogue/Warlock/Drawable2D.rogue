library Warlock

class Drawable2D : Entity2D
  PROPERTIES
    color  = Color.WHITE : Color
    shader : Shader

  METHODS
    method draw
      draw( position )

    method draw( position:XY )
      draw( Box(position,size).relative_to( anchor ) )

    method draw( bounds:Box )
      println "TODO: $.draw(Box)"(type_name)

    method on_draw

    method on_end_draw
endClass
