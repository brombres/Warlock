library Warlock

class Drawable2D : Entity2D
  PROPERTIES
    color  = Color.WHITE : Color
    shader : Shader

  METHODS
    method init( existing:Drawable2D )
      prior.init( existing )
      color  = existing.color
      shader = existing.shader
endClass
